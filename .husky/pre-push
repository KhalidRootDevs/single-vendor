BRANCH="$(git rev-parse --abbrev-ref HEAD)"

# Only build for main branch
if [ "$BRANCH" = "main" ]; then
  echo "Building on main branch..."
  npm run build
else
  echo "Skipping build - not on main branch (current: $BRANCH)"
  exit 0
fi